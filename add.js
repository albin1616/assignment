/**
 * 
 * @param {number} a must be a number
 * @param {number} b munst be a number
 * @returns the result
 */
function additioon(a,b){
    const ans=a+b;
    // return answer where the result stored;
    return ans;
}